#!/bin/bash

source environment

if [ $# -gt 0 ]; then
    PACKAGES=$@

    for PACKAGE in $PACKAGES; do
        ${STOW} -d $STOWDIR -t $TARGET -S $PACKAGE
    done
else
    ${STOW} -d $STOWDIR -t $TARGET -S $PACKAGES
fi

